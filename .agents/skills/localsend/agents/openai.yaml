interface:
  display_name: "Localsend"
  short_description: "Repo-specific patterns and workflows for localsend"
  default_prompt: "Use the localsend repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true